# match-odds environment template.
# Copy to `.env` (gitignored) and adjust. No secrets are required for local development yet.
#
# Reserved for later epics (e.g. the inference service in Epic 05):
# SERVING_HOST=0.0.0.0
# SERVING_PORT=8000
