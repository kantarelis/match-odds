-r requirements.txt

pytest>=8.3,<9
pytest-cov>=6.0,<7
coverage-badge==1.1.2
# coverage-badge imports pkg_resources, which setuptools removed in 81.0.0. Pin setuptools
# below that cutoff so `make coverage-badge` works on fresh venvs until coverage-badge migrates.
setuptools<80
