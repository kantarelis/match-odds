# Runtime core — used by both modelling and the inference service.
# Exact resolved versions should be recorded after `make install-dev`.
pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<2
xgboost>=2.1,<3
# scipy backs the Dixon-Coles MLE (scipy.optimize / scipy.special); the service may ship that model,
# so it is a direct runtime dep (previously transitive via scikit-learn).
scipy>=1.13,<2
joblib>=1.4,<2
pyarrow>=24,<25
# Validation & config (Epic 02+): Pydantic owns API schemas, the typed Settings, and the
# Match record validated at the ingestion edge (see CLAUDE.md → Data & validation layers).
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
# HTTP client for the data-acquisition step (matchodds.data.sources).
httpx>=0.27,<1
