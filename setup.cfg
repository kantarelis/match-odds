[flake8]
max-line-length = 120
# E203 (whitespace before ':') and W503 (line break before binary operator) conflict with black.
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist,
    .mypy_cache,
    .pytest_cache,
    .ipynb_checkpoints
